import React, { useState } from 'react'
import {useParams} from 'react-router-dom'

function DeleConDele({contact}) {
    console.log(contact)
    // const [cont, setcont] = useState()
    // let { id } = useParams()

    // let dataHold = JSON.parse(localStorage.getItem('Contact-Data'))
    // console.log(dataHold)
    // let get = dataHold.filter(elem => elem.id !== id )
    
    // console.log( get )

    
  return (
    <>
          <h1>This is delete your id is __</h1>
    </>
  )
}

export default DeleConDele